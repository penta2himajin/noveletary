# noveletary — agent instructions

The canonical instructions live in [`AGENTS.md`](./AGENTS.md). Read that file.

> This file should be a symlink to `AGENTS.md` (per penta2himajin/templates), but the
> GitHub contents API cannot create symlinks. After cloning, restore the symlink to
> avoid drift:
>
> ```bash
> ln -sf AGENTS.md CLAUDE.md
> ```
